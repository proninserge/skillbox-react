import {PostControl} from '@utils/constants';

type TPostControl = PostControl.COMMENTS | PostControl.SHARE | PostControl.HIDE | PostControl.SAVE | PostControl.COMPLAIN | PostControl.CLOSE;

export default TPostControl;